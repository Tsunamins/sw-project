type Query {
  findUser: User
#   uploads: [File]
  
}

type Mutation {
  singleUpload(file: Upload!): File!
}

type User {
  id: ID!
  username: String!
  name: String!
  email: String!
  photo: File!
}

type File {
    filename: String!
    mimetype: String!
    encoding: String!
}

